class Cipher {

    static encode(source, alphabet, shift) {
        console.log(`encoding with source = ${source} alphabet = ${alphabet.uppercase} shift = ${shift}`);
        let encodedSource = alphabet;
        return encodedSource;
    }

    static decode(source, alphabet, shift) {
        console.log(`decoding with source = ${source} alphabet = ${alphabet.uppercase} shift = ${shift}`);
        return decodedSource;
    }

}

export default Cipher
